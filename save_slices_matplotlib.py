import numpy as np
import matplotlib.pyplot as plt

vp_bg = np.load("vp_all_anomalies.npy") # Just temporarily load the output to show we can use matplotlib 
# Note: we should rewrite to get slices directly in script
